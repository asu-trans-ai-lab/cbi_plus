# outputs & caches — the repo ships code + docs only, never data or run artifacts
__pycache__/
*.pyc
outputs/
*.png
*.parquet
*.zip
_inputs/
# large adapter intermediates
tfb_*.json
# local editor/OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store

# review-materials drop (raw previous versions, 131MB INRIX I-17 etc.) — reference only
additional/

# EXCEPTION: benchmark evidence figures are the deliverable — keep them tracked
!benchmarks/**/*.png
!docs/figures/*.png
!figures/*.png

# legacy-repo archive: reference only, never ship (46MB Reading.csv etc.)
old_github_repo/

# internal named team assignments — never publish
docs/INTERNAL_TEAM_ROLES.md

# packaging artifacts
build/
*.egg-info/
dist/
